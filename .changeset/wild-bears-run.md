---
"vue-demo-store": patch
---

Align `uno.config.ts` with the split CMS and design-token layers: add presets and CMS base layer color tokens so the demo store keeps parity with the shared UnoCSS setup.
