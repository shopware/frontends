# Copy this file to `.env` and adjust the values.
# Playwright loads the nearest `.env`, starting from this folder and walking up.
# Set `ENV_FILE` to load a different file name instead.

# Storefront under test.
BASE_E2E_URL=http://127.0.0.1:8080

# Storefront account used by the login, order and review tests.
# Register it in the storefront first.
USER_EMAIL=test@shopware.com
PASSWORD=shopware123
