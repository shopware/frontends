---
nav:
  title: CMS
  position: 30
---

# CMS

Recipes for Shopping Experiences: rendering a CMS page's tree of sections, blocks and slots, and mapping each node to a component.

<PageRef page="rendering.html" title="Rendering CMS Pages" sub="Understand why the CMS page arrives inside the category or landing page response rather than in its own request, how a type becomes a component name, which composables are not reactive, and where the embedded product listing goes." />
