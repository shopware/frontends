{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build", "nuxt-prepare", "^nuxt-prepare"],
      "outputs": ["dist/**", ".output/**", ".vercel/**", ".vitepress/dist/**"]
    },
    "generate": {
      "dependsOn": ["^build", "nuxt-prepare", "^nuxt-prepare"],
      "outputs": ["dist/**", ".output/**", ".vercel/**"]
    },
    "nuxt-prepare": {
      "cache": false,
      "outputs": []
    },
    "lint": {
      "cache": false,
      "outputs": [],
      "passThroughEnv": ["TURBO_TELEMETRY_DISABLED", "DO_NOT_TRACK"]
    },
    "lint:fix": {
      "cache": false,
      "outputs": [],
      "passThroughEnv": ["TURBO_TELEMETRY_DISABLED", "DO_NOT_TRACK"]
    },
    "typecheck": {
      "dependsOn": ["build", "^build", "^nuxt-prepare"],
      "outputs": [],
      "inputs": [
        "src/**/*.ts",
        "src/**/*.vue",
        "*.ts",
        "tsconfig.json",
        "package.json"
      ]
    },
    "generate-types": {
      "cache": false,
      "outputs": []
    },
    "generate-admin-types": {
      "cache": false,
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "test:watch": {
      "dependsOn": ["^build"],
      "persistent": true
    },
    "test:bench": {
      "cache": false,
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test:e2e": {
      "cache": false,
      "outputs": []
    }
  }
}
